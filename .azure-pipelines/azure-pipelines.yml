trigger:
  branches:
    include:
      - main

stages:
  - stage: Info
    jobs:
      - job: Echo
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - script: echo "Bechde CI will be wired with coverage gates in next PRs."
