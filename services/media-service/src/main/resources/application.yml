server:
  port: ${PORT:8085}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/bechde}
    username: ${DB_USER:bechde}
    password: ${DB_PASS:bechde}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: ${DB_SCHEMA:media}
  flyway:
    enabled: true
    create-schemas: true
    schemas: ${DB_SCHEMA:media}

management:
  endpoints:
    web:
      exposure:
        include: health,info
