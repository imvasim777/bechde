apiVersion: apps/v1
kind: Deployment
metadata: { name: media-service }
spec:
  replicas: {{ .Values.replicaCount }}
  selector: { matchLabels: { app: media-service } }
  template:
    metadata: { labels: { app: media-service } }
    spec:
      containers: [ { name: media-service, image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}", ports: [ { containerPort: 8080 } ] } ]
---
apiVersion: v1
kind: Service
metadata: { name: media-service }
spec:
  selector: { app: media-service }
  ports: [ { port: {{ .Values.service.port }}, targetPort: 8080 } ]
